version: '3.7'

services:
  main:
    container_name: 'api-movie'
    build: '.'
    volumes:
      - '.:/main:rw'
      - '/main/node_modules'
    environment:
      MONGO_URI: process.env.USER_BD
    ports:
      - '8000:8000'